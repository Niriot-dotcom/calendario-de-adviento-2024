export const COLLECTIONS = {
  Registro: "adviento_whatsapp",
};

export type USER_SCHEMA = {
  username: string;
  email: string;
  password: string;
  angelitaName: string;
  angelitoName: string;
  lastDay: number;
};
